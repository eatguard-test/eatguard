{
  "name": "eatguard-ai-backend",
  "version": "2.0.0",
  "description": "EatGuard AI 识别后端（Vercel Serverless）：文字→DeepSeek(回退qwen-plus)，图片→Qwen-VL。API Key 只存环境变量。",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
